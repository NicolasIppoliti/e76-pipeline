# Local disposable development credentials only. Never reuse outside this demo.
DATABASE_URL=postgresql://pipeline_admin:local-admin-only@127.0.0.1:55476/pipeline
NORTHWIND_DATABASE_URL=postgresql://pipeline_northwind:local-northwind-only@127.0.0.1:55476/pipeline
LUMEN_DATABASE_URL=postgresql://pipeline_lumen:local-lumen-only@127.0.0.1:55476/pipeline
# Integration tests reset their own pipeline/staging/reporting schemas in this database.
# Create first: docker compose exec db createdb -U pipeline_admin pipeline_test
TEST_DATABASE_URL=postgresql://pipeline_admin:local-admin-only@127.0.0.1:55476/pipeline_test
